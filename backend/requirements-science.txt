# Locked scientific runtimes for the single-cell worker (spec 10).
# Versions must match app.constants.LOCKED_METHODS; a mismatch is a release bug.
scanpy==1.10.1
anndata==0.10.8
leidenalg==0.10.2
igraph==0.11.6
gseapy==1.1.3
# The bulk statistical engine runs in the separate R worker image:
#   R + Bioconductor + DESeq2 1.42.0, reached through rpy2.
rpy2==3.5.16
