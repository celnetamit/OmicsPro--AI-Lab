# Locked scientific runtimes for the single-cell worker (spec 10).
# Versions must match app.constants.LOCKED_METHODS; a mismatch is a release bug.
scanpy==1.10.1
anndata==0.10.8
leidenalg==0.10.2
igraph==0.11.6
gseapy==1.1.3
# scikit-image is a hard requirement of the doublet detector reached through
# scanpy.pp.scrublet. It is not pulled in transitively, and without it the Core
# pipeline fails at the cell-QC step with ModuleNotFoundError.
scikit-image==0.25.2

# rpy2 is deliberately NOT here. It cannot install without R present, so
# including it made INCLUDE_SCIENCE=true unbuildable. The bulk statistical
# engine runs in the separate R worker image (R + Bioconductor + DESeq2
# 1.42.0, reached through rpy2) — see requirements-r-worker.txt.
